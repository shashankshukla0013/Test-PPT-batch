def calculate_mean(numbers):
    total = sum(numbers)
    count = len(numbers)
    mean = total / count
    return mean

data = [8 , 1, 5, 2, 3]
mean_value = calculate_mean(data)
print("Mean:", mean_value)
